import { Input } from "../utils/interfaces";

interface Props {
    input: Input;
}

function TokenManagementForm({ input }: Props) {

    return (
        <tr>

        </tr>
    );
}

export default TokenManagementForm;